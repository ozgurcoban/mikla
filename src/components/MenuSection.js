import styled from 'styled-components';

const MenuSection = () => {
  return (
    <Section>
      <h1>Miklas meny</h1>
    </Section>
  );
};

export default MenuSection;

const Section = styled.section`
  height: 80vh;
`;
