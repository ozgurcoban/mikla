import styled from 'styled-components';

const AboutSection = () => {
  return (
    <Section>
      <h1>Om Miklagård</h1>
    </Section>
  );
};

const Section = styled.section`
  height: 80vh;
`;

export default AboutSection;
